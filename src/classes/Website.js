class Website {
  constructor(
    // parameters of the constructor
    link,
    name,
    year,
    task,
  ) {
    // properties of the class
    this.link = link
    this.name = name
    this.year = year
    this.task = task
  }
}

export default Website
