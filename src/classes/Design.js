class Design {
  constructor(
    // parameters of the constructor
    link,
    name,
    year,
    program,
  ) {
    // properties of the class
    this.link = link
    this.name = name
    this.year = year
    this.program = program
  }
}

export default Design
